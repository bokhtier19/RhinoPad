{
    "buildCommand": "npm run build",
    "outputDirectory": "frontend/dist",
    "framework": null,
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}
